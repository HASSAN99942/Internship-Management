import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — Phase 1 widget tests are in features/auth/
  testWidgets('smoke test placeholder', (tester) async {
    expect(true, isTrue);
  });
}
